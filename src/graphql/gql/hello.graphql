type Query {
  # 缓存180
  hello: String @cacheControl(maxAge: 180)
  #test: String @cacheControl(maxAge: 180)
}

# type Author @cacheControl(maxAge: 60) {
#  id: Int
#  firstName: String
#  lastName: String
#  posts: [Post] @cacheControl(maxAge: 180)
# }
