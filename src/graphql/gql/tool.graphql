scalar Upload
type Query {
  # 请求验证码
  captcha: CaptchaData!
  wallPaper(search: String!): String!
}

type Mutation {
  singleUpload(file: Upload!): File!
}

type CaptchaData {
  text: String!
  img: String!
}

type ResponseData {
  code: String!
  msg: String!
}

type File {
  url: String!
}
